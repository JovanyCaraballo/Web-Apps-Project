class Grid{
    constructor(
      numberOfColumns,
      numberOfRows,
      cards,
      gridStyle
    ){
      this.numberOfColumns = numberOfColumns,
      this.numberOfRows = numberOfRows, 
      this.cards = cards,
      this.gridStyle = gridStyle
    };
  
  }

  export default Grid;
